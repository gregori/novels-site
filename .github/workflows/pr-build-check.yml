name: PR Build Check

# BR-5: validates every Pull Request's build (front-matter/type errors, etc.)
# without deploying anything — only a push to main triggers a deploy.
on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build (type-check + production build)
        run: npm run build
